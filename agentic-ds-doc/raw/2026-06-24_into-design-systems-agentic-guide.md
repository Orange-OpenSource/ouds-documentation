# Agentic Design Systems: The Complete Guide

**Source** : https://www.intodesignsystems.com/agentic-design-systems
**Auteur** : Into Design Systems (synthèse de 5 speakers à l'AI Design Systems Conference 2026)
**Speakers** : Romina Kavcic (The Design System Guide), Diana Wolosin (Indeed), Jesse Gardner (New York State), Jan Six (GitHub), Brad Frost (Brad Frost Web) + Ian Frost, TJ Pitre (Southleft)
**Date** : 2026 (conférence mars 2026)

---

## Définition

An agentic design system is infrastructure that lets AI agents autonomously read, reason over and build with your components, tokens and guidelines. Unlike traditional design systems written for humans, agentic design systems encode intent, relationships and constraints as machine-readable context. Agents can then observe, detect, suggest, fix and learn inside a self-healing loop with human oversight.

## Key concepts

- **Design system as infrastructure:** Think of your design system less like a Figma library and more like a CI/CD pipeline or database. It becomes the API that lets AI agents build your product safely.
- **Observe, detect, suggest, fix, learn:** A self-healing loop based on IBM's 2003 MAPE-K framework. Signals from Figma API, CI hooks and usage analytics flow into a drift-scoring engine that creates PRs automatically.
- **AI is a new user:** Your design system was written for humans. The new user, AI, needs structured metadata, not documentation prose.
- **MCP is deterministic, LLM is stochastic:** The same MCP query returns the same data, but the LLM reasons differently each time. Structured data like JSON mitigates that randomness.
- **Context > Probability:** Without infrastructure, AI collapses toward the average of the internet. Design systems give AI something it cannot get from training data: your encoded decisions.
- **Trust levels, not full autonomy:** Agents earn trust like career progression. Start as an intern (suggest only), then junior (mechanical fixes), then more autonomy. Some decisions will always need human judgment.
- **Plant seeds, not trees:** Foundations before automation.
- **Progressive disclosure of context:** Always-on rules for foundations, MCP on-demand for components, AGENTS.md as orchestration layer.

## Why now (stats from conference)

- 28 frontier models shipped in the last 9 months
- $200B+ invested in AI over the last year
- 100k → 8M+ MCP downloads growth in under a year
- 40% of enterprises will use AI agents by end of year (Gartner)
- 4,300 AI prototypes generated by Indeed in 4 months using their design system MCP
- 5x cheaper: JSON vs Markdown for the same MCP queries ($300 vs $1,500 per year)

## Real-world examples

**Indeed (Diana Wolosin):** Parsed 77 components from MDX docs into JSON metadata via Cursor-built parsers, ingested into a Vectra vector database, ran a benchmark with 1,056 prompts across 8 MCP configurations. JSON won on both accuracy and token efficiency. Pipeline auto-triggers on MDX updates. Pilot produced 4,300 AI prototypes in 4 months.

**New York State (Jesse Gardner):** Design system built on Lit and TypeScript web components with Figma parity via Code Connect. Custom MCP server exposes components and tokens documented via JSDoc with usage guidance baked in. Fed a 5-page foster-adoptive parent PDF into Claude Code to generate a working multi-step NY State-styled form in 13 minutes.

**GitHub (Jan Six):** GitHub Primer has a public MCP and instruction files. Workflow: start in Storybook, push to preview env, invite review. Uses sub-agents like an accessibility reviewer to keep main context clean. Agentic workflows run daily QA and maintenance with safe outputs, where an agent can only create an issue.

**The Design System Guide (Romina Kavcic):** Built Tidy, a Figma plugin with 66 MCP tools that audits naming, scores health across 6 categories, validates new variables and composes patterns from existing components. Paired with Observatory, a dashboard that visualizes the knowledge graph across Figma, GitHub, Storybook, Linear, Chromatic, Playwright and PostHog.

**Brad Frost Web & Southleft (Brad Frost, Ian Frost, TJ Pitre):** FigmaLint plugin runs deterministic and AI analysis on Figma components, scores them (26/100 up to 100/100 after fixes), checks token usage, layer naming, accessibility and component descriptions. Figma Console MCP demo: "Create a new theme called Lavender" instantly rolls out new tokens to Figma, Storybook and React Native.

**Spotify Encore:** Layered component architecture separating foundations, styles and behaviors into independent layers. Creates smaller context bubbles for AI reasoning. 220,000+ shared style uses, 86,000 component instances, 93% developer satisfaction. Custom MCP evaluation framework testing prompts against multiple LLMs with both code and visual comparison.

## How to start

1. Plant seeds, not trees (Romina Kavcic): naming conventions, token structure, component descriptions first.
2. Make components machine-readable (Diana Wolosin, Indeed): JSON schema per component: identity, props, variants, sizes, states, usage.
3. Add intent to Figma descriptions (Romina Kavcic): write what the component does, not just specs.
4. Set trust levels per action (Romina Kavcic): low-risk + high-confidence = auto; everything else = human review.
5. Colocate and name semantically (Jan Six, GitHub): danger.background, not red-500. Add front-matter to docs.
6. Benchmark before committing to a format (Diana Wolosin, Indeed): test JSON vs MD vs hybrid.
7. Pair with a developer (Romina Kavcic): real pair programming, not just weekly syncs.

## Key quotes

- "AI generates code, design systems generate understanding." — Romina Kavcic
- "AI is a new user. And as a new user, our design system needs to be in a format that the AI can understand." — Diana Wolosin
- "The question isn't whether AI can generate interfaces, it can. The question is what infrastructure makes that output good or trustworthy." — Jesse Gardner
- "The invisible part of your system is way bigger than your visible part. If the agent can't see it, it has to hallucinate." — Jan Six
- "I see design system as an infrastructure, the same way as you can see your CI/CD pipeline or the database. It's like an API that allows AI to build your product safely." — Romina Kavcic
- "JSON is like a contract. It has explicit keys, explicit values, explicit boundaries, and there is no ambiguity." — Diana Wolosin

## FAQ key extracts

- Should I connect Markdown docs to MCP? No. JSON hit higher accuracy with 80% fewer tokens and 5x lower annual cost ($300 vs $1,500). "Our docs are written for humans. The new user (AI) needs structured metadata."
- Will AI replace design systems? The opposite. "AI generates code, design systems generate understanding. Without a strong system, AI collapses toward the average of the internet."
- Should agents run fully autonomously? No. "Some decisions will never go past level three because they'll always need human judgment."
- Why does my agent keep breaking foundations? MCP is on-demand. If the prompt says "build me a card", MCP returns card info but ignores spacing, typography, colors. Use always-on rules for foundational knowledge.
- What is AGENTS.md? Orchestration file that tells AI agents how to work with your design system: always-on rules for foundations, pointer to MCP server, trust levels per action.
