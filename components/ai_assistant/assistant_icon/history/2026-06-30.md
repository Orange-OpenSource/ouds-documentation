# Creation log — assistant_icon — 2026-06-30

New component documentation created from scratch (ouds-doc-creation workflow). Sibling of
`ai_assistant/assistant_button` (created same day).

- **Path:** `components/ai_assistant/assistant_icon/` (top-level `ai_assistant` category)
- **Figma file:** `AsLZsWCquXNUe0yIgS5ejm` (AI assistant - Assistant icon)
- **Overview node:** `16001:343`
- **Zeroheight asset frames:** Guidelines `16078:1963`, Specs `16078:2023`

## Frame classification (Overview, Y-order)

| Y | Frame (name) | Disposition |
|---|---|---|
| 48 | Changelog | → changelog mirror + master `# Changelog` |
| 337 | Definition | INJECT `## Definition` |
| 621 | Properties (Component Prop Table — State only) | ❌ Excluded |
| 948 | Initial Settings | ❌ Excluded |
| 1156 | States | INJECT → `# Specs / ## States` |
| 2089 | **States** (frame mis-named) — real title **"⚠️ Interactive icon vs button (icon only)"** | INJECT Guideline + generate do/don't |
| 3034 | Multiline and responsiveness | INJECT (no do/don't) |

No "Behaviour and accessibility" section exists in this component.

## Generated sections (research-backed)

- Intro 🤖, Best for 🤔 (10), Anatomy 🤖 (6 rows, icon-specific: no container),
  `interactive_icon_vs_button_icon_only_do_&_dont` 🤔 (5), Accessibility 🤖 (Orange template,
  icon-only emphasis: aria-label, color-only state, 28px min target, Loading announce).
- Research reused from `assistant_button` (SAP Fiori AI buttons, Carbon AI label, W3C APG Button).

## Execution checklist

- [x] Phase 0 — discover/confirm new, classify
- [x] Phase 1 — extract → `figma/` mirrors (100% fidelity, English-only)
- [x] Phase 2 — generate master `assistant_icon.md`
- [x] Phase 3 — build `dsm/` (16 slices) + validate (28/28) + verify extraction (11/11)
- [x] Phase 4a — interim export at 2× + `.md` companions
- [x] Phase 4b (Phase B) — verbatim copy of Overview `*_img` frames into zeroheight assets
- [x] Phase 4c (Phase C) — re-exported 3 section images at 2× from restyled 468/#F6F7FC frames
- [x] Phase 5 — close-out

### Phase C re-export (done 2026-06-30)

User restyled the 3 clones to 468/#F6F7FC. Re-exported at 2× (936px). Spec layout unchanged
(still sourced from `16078:2031`, 1920px). Node map:

```json
{
  "16079:146": "assistant_icon@interactive_icon_vs_button_icon_only_img.png",
  "16079:164": "assistant_icon@multiline_and_responsiveness_img.png",
  "16079:172": "assistant_icon@states_img.png"
}
```

> ✅ Done by user (2026-06-30): `16078:2031` renamed → `assistant_icon@spec_layout_spacing`,
> and the wrong leftover `assistant_button@states_img` (`16078:2778`) **deleted**. All 4 images
> re-exported from the final clean state (spec confirmed opaque).
> ⏳ Remaining (cosmetic, does NOT affect export — images mapped by node id): rename the Overview
> source frames off their `button@` prefixes and rename the mis-named "States" frame (`16001:1098`).

## Phase B — zeroheight asset copy

Page **`📎 Zeroheight assets`**. Cloned verbatim, renamed to `assistant_icon@` prefix:

| New clone | Node | Into |
|---|---|---|
| `assistant_icon@interactive_icon_vs_button_icon_only_img` | 16079:146 | Guidelines `16078:1963` |
| `assistant_icon@multiline_and_responsiveness_img` | 16079:164 | Guidelines `16078:1963` |
| `assistant_icon@states_img` | 16079:172 | Specs `16078:2023` |

- **Spec layout** not copied — the Assistant-icon redline spec already exists in Specs as
  `circular_progress_indicator@specs_layout_spacing` (`16078:2031`, wrong prefix, correct content;
  2 exhibits: resizing + Focus inset). Designer should rename it `assistant_icon@spec_layout_spacing`.

## Node → filename map (interim Phase A export, all 2×)

```json
{
  "16001:1075": "assistant_icon@states_img.png",
  "16001:1104": "assistant_icon@interactive_icon_vs_button_icon_only_img.png",
  "16001:1128": "assistant_icon@multiline_and_responsiveness_img.png",
  "16078:2031": "assistant_icon@spec_layout_spacing.png"
}
```

For Phase C re-export (after restyle): Guidelines clones `16079:146` / `16079:164`, Specs clone
`16079:172`; spec_layout stays sourced from `16078:2031`. Section images verified at 2× (1200px =
2×600); spec at 1920px (2×960, opaque — no flatten needed). `.md` companions written for all 4.

## Designer follow-ups

**Wrong frame prefixes (Overview `*_img`):**

| Current | Node | Should be |
|---|---|---|
| `button@states_img` | 16001:1075 | `assistant_icon@states_img` |
| `button@states_img` | 16001:1104 | `assistant_icon@interactive_icon_vs_button_icon_only_img` |
| `button@multiline_and_responsiveness_img` | 16001:1128 | `assistant_icon@multiline_and_responsiveness_img` |

**Other:**
- Overview frame `16001:1098` is **named "States"** but titled **"⚠️ Interactive icon vs button (icon only)"** — rename the frame to match.
- Zeroheight Specs `circular_progress_indicator@specs_layout_spacing` (`16078:2031`) → rename `assistant_icon@spec_layout_spacing`.
- ⚠️ Zeroheight Specs contains a **wrong leftover** `assistant_button@states_img` (`16078:2778`) showing the **Assistant button** (with "Label"), not the icon — should be **deleted** (the correct `assistant_icon@states_img` is now in place). Not deleted by Claude (shared-file safety) — user to remove.

## Phase C trigger
User restyles the 3 new clones (+ optionally the spec frame) to the Format-spec (width 468,
bg `#F6F7FC`, padding 40), deletes the wrong leftover, then pings to re-export.
