# Creation log — typography/code — 2026-06-30

New foundation documentation (ouds-doc-creation). One of 5 typography styles created together.

- **Path:** `components/foundation/typography/code/`
- **Figma file:** `WTPHSOM8E2YQQpWs6LzkCZ` · **Overview:** `16080:1753` · **Zeroheight:** Guidelines `16101:474`, Specs `16101:486`

## Notes / deviations
- ⚠️ "Future update!" banner (v1.0.0 in design) — skipped.
- Non-interactive → `## States` = `🚧 Not applicable`; `## Anatomy` describes the monospace type tokens.
- **Single size, monospace** — Code has only a Definition section (Properties excluded), so there are **no property sections and no do/don't** (validator warns; acceptable for a single-size style).
- Two designers in changelog.

## Sections
Definition only. Generated: Intro, Best for, Anatomy, Accessibility (code a11y — `<code>`/`<pre>`, contrast, long-line scroll).

## Images (Phase A interim, 2×) — node → file
```json
{ "16101:494": "code@spec_layout_spacing.png" }
```
- No section images (no Size/Weight sections). `spec_layout_spacing` ← Specs `16101:494` (`assistant_icon@spec_layout_spacing`, Code exhibit). Opaque.

## Phase B
No section images to copy into Guidelines `16101:474`. Spec already in Specs (wrong-named).

## Phase C
Nothing to re-export beyond the spec (already correct content). If the designer restyles, re-pull.

## Designer follow-ups
- Rename Specs `assistant_icon@spec_layout_spacing` (`16101:494`) → `code@spec_layout_spacing`.
