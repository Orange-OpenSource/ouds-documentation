**Expand link** is a UI element that allows to reveal or hide additional content throughout a text link. Link expand is rendered via a dropdown containing selectable values, and is best suited for inline contexts where minimal visual weight is required, keeping the layout clean while still allowing access to extended information.
