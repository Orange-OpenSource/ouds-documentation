The “Navigation bars” component family groups all native mobile navigation components used to structure and orient the user within an application.

It includes platform-specific patterns for Android, such as Navigation bar, App bar, Toolbar, Status bar, and other system-level navigational UI elements.