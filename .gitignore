.DS_Store
node_modules
package-lock.json
package.json
.claude
.env.claude

# Local secrets — Figma Personal Access Token (never commit)
.figma-token
*.pat

# Local-only helper scripts (machine-specific, not for remote)
/scripts/

# Local-only maintenance / update-tracking logs (not for remote)
/maintenance/